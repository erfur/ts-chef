import { AudioReport } from "./AudioMetaSchema";

export function parseMp3(b: Uint8Array, report: AudioReport): void { void b; void report; }
export function parseRiffWave(b: Uint8Array, report: AudioReport, maxTextBytes: number): void { void b; void report; void maxTextBytes; }
export function parseFlac(b: Uint8Array, report: AudioReport, maxTextBytes: number): void { void b; void report; void maxTextBytes; }
export function parseOgg(b: Uint8Array, report: AudioReport): void { void b; void report; }
export function parseMp4BestEffort(b: Uint8Array, report: AudioReport): void { void b; void report; }
export function parseAiffBestEffort(b: Uint8Array, report: AudioReport, maxTextBytes: number): void { void b; void report; void maxTextBytes; }
export function parseAacAdts(b: Uint8Array, report: AudioReport): void { void b; void report; }
export function parseAc3(b: Uint8Array, report: AudioReport): void { void b; void report; }
export function parseWmaAsf(b: Uint8Array, report: AudioReport): void { void b; void report; }
