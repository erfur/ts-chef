# Version 0.3.0 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Bump the extension and lockfile package metadata from `0.2.0` to `0.3.0` and verify the packaged manifest.

**Architecture:** Use npm's version command to update the two package metadata files consistently without creating a commit or tag. Add a package identity assertion and inspect the generated VSIX metadata.

**Tech Stack:** npm, Jest, VSCE, ZIP

## Global Constraints

- Set the project version to exactly `0.3.0`.
- Do not change dependency versions.
- Do not create a Git tag.
- Include the bump with the pending README logo restoration.

---

### Task 1: Bump And Verify Version 0.3.0

**Files:**
- Modify: `package.json:5`
- Modify: `package-lock.json:3,10`
- Modify: `test/packageContributions.test.ts:3-27`

**Interfaces:**
- Consumes: current package metadata at version `0.2.0`
- Produces: npm and VSIX package metadata at version `0.3.0`

- [ ] **Step 1: Add a failing version assertion**

Add this assertion to the existing `uses the vschef Marketplace identity` test:

```ts
version: "0.3.0",
```

- [ ] **Step 2: Verify the assertion fails**

Run: `npm test -- --runInBand test/packageContributions.test.ts`

Expected: FAIL because the received package version is `0.2.0`.

- [ ] **Step 3: Update package metadata**

Run: `npm version 0.3.0 --no-git-tag-version`

Expected: npm reports `v0.3.0`, and only the root version fields in `package.json` and `package-lock.json` change.

- [ ] **Step 4: Verify tests and package metadata**

Run: `npm test -- --runInBand test/packageContributions.test.ts`

Expected: PASS with six tests.

Run: `npm test -- --runInBand`

Expected: PASS with all tests.

- [ ] **Step 5: Package and inspect version 0.3.0**

Run: `npm run package -- --no-yarn`

Expected: package succeeds and creates `vschef-0.3.0.vsix`.

Run: `unzip -p vschef-0.3.0.vsix extension/package.json | node -e "let s=''; process.stdin.on('data',d=>s+=d).on('end',()=>{const p=JSON.parse(s); if(p.version!=='0.3.0') process.exit(1); console.log(p.version);})"`

Expected: prints `0.3.0` and exits 0.

- [ ] **Step 6: Verify the final diff**

Run: `git diff --check`

Expected: exit 0 with no whitespace errors.
