# README Logo Restoration Design

## Goal

Restore the product logo above the centered `vschef` heading in the repository README.

## Design

Reinstate the previous centered HTML image block at the start of `README.md`, using the current blue logo at `assets/logo.png`. Retain the previous `vschef logo` alternative text and 120-pixel display width.

No other README content or branding asset changes are in scope.

## Verification

Confirm that `README.md` references the existing `assets/logo.png` file and that the Markdown formatting check remains clean.
